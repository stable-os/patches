From 976697fac9060ff7cb409bd62e06780c79b075ac Mon Sep 17 00:00:00 2001
From: piano1029 <46605242+piano1029@users.noreply.github.com>
Date: Thu, 31 Aug 2023 20:08:53 +0200
Subject: [PATCH 03/14] Update a comment

---
 src/libostree/ostree-sysroot-deploy.c | 4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

diff --git a/src/libostree/ostree-sysroot-deploy.c b/src/libostree/ostree-sysroot-deploy.c
index d0f35157..966cad05 100644
--- a/src/libostree/ostree-sysroot-deploy.c
+++ b/src/libostree/ostree-sysroot-deploy.c
@@ -2034,7 +2034,9 @@ install_deployment_kernel (OstreeSysroot *sysroot, int new_bootversion,
 
   /* XXX The SYSLINUX bootloader backend actually parses the title string
    *     (specifically, it looks for the substring "(ostree"), so further
-   *     changes to the title format may require updating that backend. */
+   *     changes to the title format may require updating that backend.
+   *
+   *     ! This is completely ignored in this version */
   g_autoptr (GString) title_key = g_string_new ("");
 
   if (deployment.staged) {
-- 
2.35.1

